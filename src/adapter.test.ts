import { assert } from 'chai';
import { newAdapter } from './adapter';
import * as sinon from 'sinon';

describe('Mongo Adapter', () => {
    afterEach(() => {
        sinon.restore;
    });

    it.skip('should create new adapter', () => {
        // TODO
    });
});
